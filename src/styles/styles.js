import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  form: {
    width: '100%',
    maxWidth: 800,
    margin: '0px auto',
  },
  section: {
    marginTop: 10,
    marginBottom: 10,
  },
  error: {
    color: '#f04040',
  },
});

export default useStyles;
